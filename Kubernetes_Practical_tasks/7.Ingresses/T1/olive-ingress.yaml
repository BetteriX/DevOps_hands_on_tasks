apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: olive-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: olive.k8slab.net
      http:
        paths:
          - backend:
              service:
                name: olive-svc
                port:
                  number: 80
            path: /
            pathType: Prefix
status:
  loadBalancer: {}
